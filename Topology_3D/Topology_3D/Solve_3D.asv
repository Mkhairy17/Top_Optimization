clc
clear all
%% User inputs
Lx = input('length of x=');
Ly = input('length of y=');
Lz = input('length of z=');
nx = input('nx=');
ny = input('ny=');
nz = input('nz=');
ne = nx*ny*nz;
a = Lx/nx; %elemeny width
b = Ly/ny; %element length
c = Lz/nz; %element height
% Parameters
P = 0;
rho = ones(ne,1);
while (1)
[C,dC] = sensitivity_analysis_3D(rho_old);
rho_new = MMA_3D(nelx,nely,nelz,x,volfrac,dc);
Error = norm(rho_old - rho_new,'inf');
end

