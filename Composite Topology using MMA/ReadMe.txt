the MMA algorithm does not work on this problem. 

it stuck in closed loop without reaching a solution. 
that's why the next step is to use a numerical solver on each element for the lamination parameters and the density 